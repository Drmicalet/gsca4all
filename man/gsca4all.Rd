% gsca_ridge.Rd
% Generated by roxygen2: do not edit by hand

\name{gsca4all}
\alias{gsca4all}
\title{
GSCA with Elastic Net Regularization
}
\usage{
gsca4all(data, measurement_model, structural_model, alpha = 0.1,
           max_iter = 500, tol = 1e-6, verbose = FALSE, bootstrap = FALSE,
           bootstrap_samples = 500)
}
\arguments{
  \item{data}{A data frame containing the indicator variables}

  \item{measurement_model}{A list specifying the measurement model, where each
      element is a character vector of indicator names for a construct}

  \item{structural_model}{A list specifying the structural model, where each
      element is a character vector of predictor constructs for an endogenous construct}

  \item{alpha}{Elastic Net mixing parameter (0 = Ridge, 1 = Lasso). Default is 0.1.}

  \item{max_iter}{Maximum number of iterations for the ALS algorithm}

  \item{tol}{Convergence tolerance}

  \item{verbose}{Whether to print progress information}

  \item{bootstrap}{Whether to perform bootstrapping for standard errors (default: FALSE)}

  \item{bootstrap_samples}{Number of bootstrap samples if bootstrapping (default: 500)}
}
\value{
A list containing:
\item{FIT}{Fit index (proportion of variance accounted for)}
\item{loadings}{Estimated loadings matrix}
\item{path_coefs}{Estimated path coefficients}
\item{component_scores}{Estimated component scores}
\item{weights}{Final outer weights}
\item{iterations}{Number of iterations until convergence}
\item{reliability}{Reliability measures (rho_DG, AVE)}
\item{model_spec}{Model specification used}
\item{bootstrap_results}{Bootstrap results if requested}
\item{mahalanobis}{Mahalanobis distance test results including outlier detection}
}
\description{
Fits a Generalized Structured Component Analysis (GSCA) model with Ridge
regularization to the provided data.
}
\details{
The function implements GSCA with Elastic Net regularization through an Alternating Least Squares (ALS) algorithm.
It applies Elastic Net penalty to improve stability, perform variable selection, and reduce multicollinearity in parameter estimation.
The algorithm iteratively updates component scores and outer weights until convergence.

Key features include:
\itemize{
  \item{Elastic Net regularization for stable parameter estimation and variable selection}
  \item{Comprehensive reliability assessment (rho_DG, AVE)}
  \item{Discriminant validity testing}
  \item{Bootstrap functionality for statistical inference}
  \item{Multivariate outlier detection using Mahalanobis distance}
  \item{Complete model fit indices (FIT, SRMR)}
}
}
\examples{
# Load the package
# Load the package
library(gsca4all)

# Prepare data
data <- read.csv("noviICT190.csv", row.names = 1)

# Define model structure for Model A+
model_spec_Aplus <- list(
  Knowledge = c('K1TEXTS', 'K2PICS', 'K3FILES', 'K4LINKS', 
               'K5VOCALLS', 'K6VICALLS', 'K7CRMINT', 'K8LOGS'),
  Improvement = c('BI1GEN', 'BI2s2s', 'BI3s2g'),
  Information = c('IN1TECINFO', 'IN2TECTRA'),
  Productivity = c('PR01s2s', 'PR02s2g'),
  Satisfaction = c('SA01s2s', 'SA02s2g')
)

structural_model_Aplus <- list(
  Improvement = "Knowledge",
  Productivity = c("Improvement", "Information"),
  Satisfaction = c("Improvement", "Information")
)

# Fit Model A+ with Ridge regularization
results_Aplus <- gsca_ridge(
  data = data,
  measurement_model = model_spec_Aplus,
  structural_model = structural_model_Aplus,
  alpha = 0.1,
  max_iter = 500,
  tol = 1e-6
)

# View results
print(results_Aplus)
summary(results_Aplus)

# Define model structure for Model B
model_spec_B <- list(
  Culture = c('FOSS', 'LINUX', 'PMSCRM'),
  Data = c('BI', 'DB', 'SasB'),
  CRM_Comms = c('IPPBX', 'EMAIL', 'MESSENGER'),
  Infrastructure = c('NETWORK', 'CLOUD', 'SECURITY'),
  ICT_Maturity = c('STRATEGIC', 'IMPROVEMENT', 'INNOVATION')
)

structural_model_B <- list(
  Data = "Culture",
  CRM_Comms = "Culture",
  ICT_Maturity = c("Data", "CRM_Comms", "Infrastructure", "Culture")
)

# Fit Model B with Ridge regularization
results_B <- gsca_ridge(
  data = data,
  measurement_model = model_spec_B,
  structural_model = structural_model_B,
  alpha = 0.1,
  max_iter = 500,
  tol = 1e-6
)

# View results
print(results_B)
summary(results_B)
}
\references{
Hwang, H., & Takane, Y. (2014). Generalized structured component analysis: A component-based approach to structural equation modeling. CRC Press.

Mayol-Tur, M. (2025). gsca4all: Generalized Structured Component Analysis with Elastic Net Regularization (Version 1.0.0) [Computer software]. https://doi.org/10.5281/zenodo.17155474
}
\author{
Dr. Miguel Mayol-Tur Ph.D. <m00mayoltur@gmail.com>
Maintainer: Dr. Miguel Mayol-Tur Ph.D. <m00mayoltur@gmail.com>
}
\seealso{
\code{\link{print.gsca4all}}, \code{\link{summary.gsca4all}}, \code{\link{plot.gsca4all}}
}
\keyword{multivariate}
\keyword{htest}
\keyword{models}